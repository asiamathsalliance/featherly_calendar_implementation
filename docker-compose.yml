services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: featherly
    ports:
      - "5432:5432"
    volumes:
      - featherly_pg:/var/lib/postgresql/data

volumes:
  featherly_pg:
